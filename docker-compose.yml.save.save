gunicorn/workers/base.py", line 134, in init_processpython manage.py migrate && 
                          gunicorn git_commands.wsgi:application --bind 0.0.0.0:8000"
        nginx:
                build: ./nginx
                container_name: nginx
                links:
                        - web
                depends_on:
                        - web
                volumes:
                        - static_volume:/app/staticfiles
                        - media_volume:/app/media
                ports:
                        - "80:80"
volumes:
  media_volume:
  static_volume:
